module.exports = function(app) {
    var kidController = require('../controllers/kid.js');

    // Create a new kid
    //app.post('/kid', kidController.create);

    // Retrieve all kids
    //app.get('/kids', kidController.findAll);

    // Retrive a single kid with kid id
    //app.get('/kid/:kidId', kidController.findOne);

    // Update an kid with kid id
    //app.put('/kid/:kidId', kidController.update);

    // Delete an kid with kid id
    //app.delete('/kids/:kidId', kidController.delete);
}